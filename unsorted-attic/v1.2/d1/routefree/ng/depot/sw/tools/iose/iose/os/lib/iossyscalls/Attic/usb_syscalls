IOSError FindDevice(u16 vendorId, u16 productId, char *buf, int bufsize);
IOSError FindDeviceIds(u16 vendorId, u16 productId, IOSCid *fd, IOSCid *qid);
IOSError RegisterResourceManager(IOSCid fd);
IOSError UnregisterResourceManager(IOSCid fd);

